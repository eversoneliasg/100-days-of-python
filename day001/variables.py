# The variable will store the data
name = input("What is your name?")
print(name)
# We can change the value atributed to a variable
name = "Angela"
print(name)
# We can use variables to simplify commands, like the one we saw earlier
length = len(name)
print(length)
# Naming variables
  # make your code readable with meaningful names
  # don't use space, use underscore, like user_name
  # can't begin with numbers, for example 2length
  # don't use privileged words, like functions names
